query ($id: String!) {
              userById(id: $id) {
                success
                status
                result {
                  id
                  username
                  email
                  emailConfirmed
                }
                title
                message
              }
            }