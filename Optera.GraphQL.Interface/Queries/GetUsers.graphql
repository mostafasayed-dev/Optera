query ($userParams: UserParamsInput!) {
              users(userParams: $userParams) {
                success
                status
                result {
                  id
                  username
                  email
                  emailConfirmed
                }
                title
                message
              }
            }